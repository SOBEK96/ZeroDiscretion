[project]
name = "zero-discretion"
version = "1.0.0"
description = "Autonomous zero-discretion bug bounty escrow protocol on GenVM"
readme = "README.md"
license = { text = "MIT" }
authors = [{ name = "SOBEK96", email = "btcehsan@yahoo.com" }]
requires-python = ">=3.12"
dependencies = []

[project.optional-dependencies]
dev = [
    "genlayer-test==0.30.0rc2",
    "genvm-linter==0.11.1rc2",
    "pytest>=9.0",
]

[tool.pytest.ini_options]
testpaths = ["tests/direct"]
pythonpath = ["tests/direct"]
